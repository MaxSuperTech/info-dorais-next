---
import PageContent from "@/components/PageContent.astro";
import { getStaticPages, type PageKey } from "@/data/site";
import BaseLayout from "@/layouts/BaseLayout.astro";

export function getStaticPaths() {
  return getStaticPages("fr").map(({ key, slug }) => ({
    params: { slug },
    props: { pageKey: key },
  }));
}

const { pageKey } = Astro.props as { pageKey: PageKey };
---

<BaseLayout locale="fr" pageKey={pageKey}>
  <PageContent locale="fr" pageKey={pageKey} />
</BaseLayout>
